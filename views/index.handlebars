<div class="container">
    <div class="row header">
        <div class="col-md-3">
            <img class="#" src="">
        </div>
        <div class="col-md-6">
            <h1>Steal the Burgers!</h1>
        </div>
        <div class="col-md-3">
            <img class="#" src="">
        </div>
    </div>

    <div class="row input">
        <div class="col-md-12">
            <h4>Steal the burgers by clicking "Steal". Want to steal a burger that isnt there? Replace the placeholder text with it's name and click "Submit"</h4>
            <div class="input-group mb-3">
                <input id="usersBurger" type="text" class="form-control" placeholder="Burger Name" aria-describedby="basic-addon2">
                <div class="input-group-append">
                    <button class="btn btn-secondary submit" type="button">Submit</button>
                </div>
            </div>
            <br>

        </div>
    </div>

    <div class="row">
        <div class="col-md-6 burgers">
            {{#each burgers}}
            {{#unless devoured}}
            {{> burger/burger-block devoured=false}}
            {{/unless}}
            {{/each}}
        </div>
        <div class="col-md-6">
            {{#each burgers}}
            {{#if devoured}}
            {{> burger/burger-block devoured=true}}
            {{/if}}
            {{/each}}
        </div>
    </div>
</div>